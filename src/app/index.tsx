import { Text, View } from "react-native";

const index = () => {
  return (
    <View className="flex-1 border-2 border-red-500">
      <Text className="text-lg text-red-500">index</Text>
    </View>
  );
};

export default index;
